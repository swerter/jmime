Installation

Make sure you have the following Debian packages installed:

  - build-essentials
  - libgmime-2.6-dev
  - libxapian-dev

Download, build and install Google's gumbo-parser library to the system:

    https://github.com/google/gumbo-parser


Clone this repo recursively to fetch the submodule repo Parson (JSON library).

  git clone --recursive git@github.com:dejanstrbac/jmime.git


In the jmime directory:

  make


The _build directory has now the JMime tools.